# Configuration de la base de données
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=maplenou

# Configuration JWT
JWT_SECRET=change-me-in-prod-very-secure-secret-key

# Configuration CORS
CORS_ORIGIN=http://localhost:5173

# Configuration du produit
STOCK_DAILY_CAP=100
RESERVATION_CUTOFF_MINUTES=30

# Configuration du serveur
PORT=5000
NODE_ENV=development
